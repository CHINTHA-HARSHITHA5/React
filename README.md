# React
This project is a simple React application built using JavaScript functional components. It demonstrates how to manage data with useState, handle side effects with useEffect, and structure UI using reusable components. It is a beginner-friendly project that shows how React updates the UI dynamically based on state changes.
